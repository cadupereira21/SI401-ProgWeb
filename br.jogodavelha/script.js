// 0 = circulo, 1 = xis, -1 = vazio

var tabela = new Array(3);

tabela[0] = [-1, -1, -1];
tabela[1] = [-1, -1, -1];
tabela[2] = [-1, -1, -1];

var vez = 0;

function jogada(vez, celula){
    
    /*
    * Caso vez seja do circulo
    *   
    *   1)  
    * */
    
    switch (vez){
        case 0:
            break;
        case 1:
            break;
    }
}